import { Check } from '../check';
declare type Subset<_T extends U, U> = U;
declare type ConfigInput = Subset<Check.State, {
    cache_file: string;
}>;
export declare class Config {
    private readonly state;
    private readonly defaultSchema;
    static new(state: ConfigInput, schema?: Check.Schema): Promise<Config>;
    constructor(state: ConfigInput, defaultSchema: Check.Schema);
    checkCache(newState: Check.State): Promise<Check.Schema | undefined>;
    set(update: Partial<Check.Schema>): Promise<void>;
    all(): Promise<Check.Schema | undefined>;
    get<K extends keyof Check.Schema>(key: K): Promise<Check.Schema[K] | undefined>;
    reset(): Promise<void>;
    delete(): Promise<void>;
}
export {};
