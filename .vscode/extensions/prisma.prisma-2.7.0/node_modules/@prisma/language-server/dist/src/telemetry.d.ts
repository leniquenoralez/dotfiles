import { IConnection } from 'vscode-languageserver';
export interface TelemetryPayload {
    action: string;
    attributes: {
        [key: string]: any;
    };
}
export interface ExceptionPayload {
    signature: string;
    message: string;
    stack: string;
}
export declare const initializeTelemetry: (serverConnection: IConnection) => void;
export declare const sendTelemetry: (payload: TelemetryPayload) => void;
export declare const sendException: (signature: string, error: Error, message?: string) => void;
